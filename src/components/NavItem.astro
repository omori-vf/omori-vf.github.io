---
export interface Props {
  title: string;
  link: string;
}

const { title, link } = Astro.props;
---

<li>
  <a data-text={title} href={link}>{title}</a>
</li>

<style>
  li {
    list-style-type: none;
  }

  li > a {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    color: inherit;
    text-decoration: none;
    cursor: url("/images/icons/cursor_selected.png"), auto;
  }

  li > a:visited {
    color: var(--white);
  }

  li > a:hover {
    font-weight: var(--font-weight-bold);
  }

  li > a::after {
    content: attr(data-text);
    height: 0;
    visibility: hidden;
    overflow: hidden;
    user-select: none;
    pointer-events: none;
    font-weight: var(--font-weight-bold);
  }

  @media speech {
    li > a::after {
      display: none;
    }
  }
</style>
